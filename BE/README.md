# Loyalty App (Express + MongoDB)

## Quick start
1. Copy `.env.example` to `.env` and set `MONGO_URI`.
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start the app:
   ```bash
   npm start
   ```
4. API:
   - `POST /api/customers` create customer
   - `GET /api/customers` list customers
   - `GET /api/customers/:id` get customer
   - `POST /api/transactions` create transaction (updates points)

This project was generated for you by ChatGPT.
